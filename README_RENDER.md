# Lab Management System - Render Deployment

## ✅ تم إعداد المشروع للنشر على Render

تم تحديث جميع الملفات اللازمة لجعل التطبيق يعمل على Render.

## 📋 المتطلبات

1. **حساب Render** - سجل في https://render.com
2. **قاعدة بيانات Neon** - أنشئ قاعدة بيانات PostgreSQL على https://neon.tech
3. **مستودع Git** - ادفع الكود إلى GitHub/GitLab/Bitbucket

## 🚀 خطوات النشر

### 1. إعداد قاعدة البيانات

1. اذهب إلى https://neon.tech وأنشئ حساب
2. أنشئ مشروع جديد وقاعدة بيانات PostgreSQL
3. انسخ connection string (DATABASE_URL)
   - الصيغة: `postgresql://user:password@host/database?sslmode=require`

### 2. إعداد Render

1. اذهب إلى https://render.com وسجل الدخول
2. اضغط على "New +" → "Web Service"
3. اربط مستودع Git الخاص بك
4. Render سيكتشف تلقائياً ملف `render.yaml`

### 3. إعداد متغيرات البيئة

في لوحة تحكم Render، أضف المتغيرات التالية:

- **DATABASE_URL**: connection string من Neon
- **SESSION_SECRET**: يمكنك ترك Render يولدها تلقائياً، أو ضع قيمة مخصصة
- **NODE_ENV**: `production` (موجود بالفعل في render.yaml)

### 4. النشر

Render سيقوم تلقائياً بـ:
- تثبيت الحزم (`npm install`)
- بناء المشروع (`npm run build`)
- تشغيل الخادم (`npm start`)

## 📝 ملاحظات مهمة

1. **المنفذ**: Render يوفر متغير `PORT` تلقائياً - لا حاجة لإعداده يدوياً
2. **قاعدة البيانات**: تأكد من أن connection string صحيح ومتصل
3. **الجلسات**: `SESSION_SECRET` مهم للأمان - لا تشاركه علناً

## 🔧 استكشاف الأخطاء

إذا فشل البناء:
- تحقق من سجلات البناء في Render dashboard
- تأكد من أن `DATABASE_URL` صحيح
- تحقق من أن جميع الحزم موجودة في `package.json`

إذا فشل التشغيل:
- تحقق من سجلات التطبيق
- تأكد من اتصال قاعدة البيانات
- تحقق من أن المنفذ متاح

## 📞 الدعم

للمساعدة، راجع:
- Render Docs: https://render.com/docs
- Neon Docs: https://neon.tech/docs
