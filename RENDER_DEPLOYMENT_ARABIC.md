# 🚀 دليل نشر التطبيق على Render

## نشر سريع إلى Render

### الطريقة الأولى: النشر التلقائي (موصى به)

1. **رفع الكود إلى GitHub:**
   ```bash
   git init
   git add .
   git commit -m "Initial commit - Lab Management System"
   git branch -M main
   git remote add origin https://github.com/اسم_المستخدم/اسم_المشروع.git
   git push -u origin main
   ```

2. **النشر على Render:**
   - اذهب إلى [لوحة تحكم Render](https://dashboard.render.com/)
   - اضغط **"New +"** → **"Blueprint"**
   - اربط حساب GitHub الخاص بك
   - سيقوم Render تلقائياً بإعداد كل شيء باستخدام ملف `render.yaml`

3. **إعداد متغيرات البيئة:**
   - سيقوم Render بإنشاء قاعدة بيانات PostgreSQL تلقائياً
   - انسخ **External Database URL** من لوحة تحكم قاعدة البيانات
   - اذهب إلى خدمة الويب → تبويب **Environment**
   - اضبط `DATABASE_URL` على رابط قاعدة البيانات
   - سيتم إنشاء `SESSION_SECRET` تلقائياً

4. **الوصول إلى تطبيقك:**
   - سيكون التطبيق متاحاً على: `https://lab-management-system.onrender.com`
   - تسجيل الدخول: **اسم المستخدم:** `KAROZH` / **كلمة المرور:** `Karoj1996`

---

### الطريقة الثانية: النشر اليدوي

#### الخطوة 1: إنشاء قاعدة بيانات PostgreSQL

1. اذهب إلى لوحة تحكم Render → **New +** → **PostgreSQL**
2. الإعدادات:
   - **الاسم:** `lab-management-db`
   - **قاعدة البيانات:** `labmanagement`
   - **المستخدم:** `labadmin`
   - **المنطقة:** Oregon (US West)
   - **الخطة:** Free
3. اضغط **Create Database**
4. انسخ **External Database URL** (يبدأ بـ `postgresql://`)

#### الخطوة 2: إنشاء خدمة الويب

1. اذهب إلى لوحة تحكم Render → **New +** → **Web Service**
2. اربط مستودع GitHub الخاص بك
3. الإعدادات:
   - **الاسم:** `lab-management-system`
   - **المنطقة:** Oregon (US West)
   - **الفرع:** `main`
   - **بيئة التشغيل:** Node
   - **أمر البناء:** `npm install && npm run build`
   - **أمر البدء:** `npm start`
   - **الخطة:** Free

#### الخطوة 3: ضبط متغيرات البيئة

في خدمة الويب، اذهب إلى تبويب **Environment** وأضف:

| المفتاح | القيمة |
|--------|-------|
| `NODE_ENV` | `production` |
| `PORT` | `10000` |
| `DATABASE_URL` | `postgresql://user:password@host:port/database` (من الخطوة 1) |
| `SESSION_SECRET` | (نص عشوائي أو دع Render ينشئه تلقائياً) |

#### الخطوة 4: النشر

1. اضغط **Create Web Service**
2. انتظر اكتمال البناء (~3-5 دقائق)
3. سيكون تطبيقك متاحاً على: `https://اسم-الخدمة.onrender.com`

---

## 📋 الإعداد بعد النشر

### تهيئة قاعدة البيانات

سيقوم Render تلقائياً بتشغيل الترحيلات عند النشر الأول. إذا لزم الأمر يدوياً:

```bash
# من Shell في Render (خدمة الويب → تبويب Shell)
npm run db:push
```

### تهيئة الاختبارات الافتراضية (اختياري)

بعد تسجيل الدخول لأول مرة:
1. اذهب إلى صفحة **الإعدادات**
2. اضغط **"إضافة 68 اختبار افتراضي (بما في ذلك تحليل البول)"**
3. سيتم إضافة جميع الاختبارات الطبية إلى قاعدة البيانات

---

## ⚙️ تفاصيل الإعدادات

### إعدادات البناء

**أمر البناء:**
```bash
npm install && npm run build
```

هذا الأمر:
1. يثبت جميع التبعيات
2. يبني الواجهة الأمامية React مع Vite
3. يجمع الخلفية Express مع esbuild
4. ينتج إلى مجلد `dist/`

**أمر البدء:**
```bash
npm start
```

يشغل: `NODE_ENV=production node dist/index.js`

### شرح متغيرات البيئة

| المتغير | الوصف | مطلوب |
|---------|-------|-------|
| `DATABASE_URL` | رابط اتصال PostgreSQL | ✅ نعم |
| `SESSION_SECRET` | مفتاح سري لتشفير الجلسة | ✅ نعم |
| `NODE_ENV` | اضبطه على `production` | ✅ نعم |
| `PORT` | رقم المنفذ (Render يستخدم 10000) | ✅ نعم |

### فحص الصحة

- **المسار:** `/api/session`
- **المتوقع:** حالة 200 OK
- **الفحص:** الخادم يعمل ويستجيب

---

## 🔧 حل المشاكل

### المشاكل الشائعة

#### 1. فشل الاتصال بقاعدة البيانات

**الخطأ:** `Failed to connect to database`

**الحل:**
- تحقق من صحة `DATABASE_URL`
- تأكد من أن قاعدة البيانات تعمل في لوحة تحكم Render
- تأكد من استخدام External Database URL (وليس Internal)

#### 2. فشل البناء

**الخطأ:** فشل `npm install` أو `npm run build`

**الحل:**
```bash
# تحقق من إصدار Node (يجب أن يكون 18+)
node --version

# امسح الذاكرة المؤقتة وأعد البناء
npm cache clean --force
npm install
npm run build
```

#### 3. مشاكل الجلسة/تسجيل الدخول

**الخطأ:** لا يمكن تسجيل الدخول أو تنتهي الجلسة فوراً

**الحل:**
- تحقق من ضبط `SESSION_SECRET`
- تأكد من تفعيل ملفات تعريف الارتباط في المتصفح
- تحقق من وحدة تحكم المتصفح لأخطاء CORS

---

## 🚀 تحسين الأداء

### قيود الخطة المجانية

تشمل خطة Render المجانية:
- **خدمة الويب:** 750 ساعة/شهر، تتوقف بعد 15 دقيقة من عدم النشاط
- **PostgreSQL:** تنتهي بعد 90 يوماً، سعة 1GB
- **لا نطاقات مخصصة** على الخطة المجانية

### منع التوقف (اختياري)

استخدم خدمة مثل UptimeRobot لإرسال طلبات كل 10 دقائق:
- رابط المراقبة: `https://your-app.onrender.com/api/session`
- الفاصل الزمني: 10 دقائق
- هذا يبقي تطبيقك "دافئاً"

### الترقية إلى خطة مدفوعة

للاستخدام الإنتاجي، ضع في اعتبارك:
- **خطة Starter ($7/شهر):** لا توقف، نطاقات مخصصة
- **خطة PostgreSQL ($7/شهر):** قاعدة بيانات دائمة، بدون انتهاء

---

## 📊 المراقبة والسجلات

### عرض السجلات

1. اذهب إلى لوحة تحكم Render
2. اختر خدمة الويب الخاصة بك
3. اضغط على تبويب **Logs**
4. عرض السجلات في الوقت الفعلي

### رسائل السجلات الرئيسية

```
✅ Database connection successful
✅ Server successfully started on port 10000
🚀 Starting server initialization...
```

---

## 📱 الوصول إلى تطبيقك

### بيانات تسجيل الدخول الافتراضية

- **اسم المستخدم:** `KAROZH`
- **كلمة المرور:** `Karoj1996`

⚠️ **مهم:** غيّر هذه البيانات في الإنتاج بتعديل `/workspace/routes.ts`

### رابط التطبيق

بعد النشر، سيكون تطبيقك متاحاً على:
```
https://lab-management-system.onrender.com
```

أو نطاقك المخصص إذا تم إعداده.

---

## ✅ قائمة التحقق من النشر

قبل الإطلاق:

- [ ] رفع الكود إلى GitHub
- [ ] إنشاء قاعدة بيانات PostgreSQL
- [ ] إنشاء خدمة الويب وربطها بالمستودع
- [ ] ضبط متغير البيئة `DATABASE_URL`
- [ ] ضبط متغير البيئة `SESSION_SECRET`
- [ ] اكتمال البناء بنجاح
- [ ] التطبيق متاح عبر الرابط
- [ ] تسجيل الدخول يعمل بالبيانات
- [ ] تهيئة مخطط قاعدة البيانات
- [ ] إضافة الاختبارات الافتراضية (اختياري)
- [ ] فحص الصحة ينجح

---

## 🎉 تطبيقك الآن مباشر!

نظام إدارة المختبرات الخاص بك يعمل الآن على Render!

**الخطوات التالية:**
1. سجل الدخول واختبر جميع الميزات
2. أضف الاختبارات الطبية عبر الإعدادات
3. ابدأ تسجيل المرضى
4. أنشئ التقارير

**جاهز للإنتاج:** ✅

للمشاكل أو الأسئلة، تحقق من السجلات أولاً، ثم راجع وثائق Render.
